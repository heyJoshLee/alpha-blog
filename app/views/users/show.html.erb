<h1 class="text-center"><%= @user.username %></h1>

<%= gravatar_for @user %>

<% if logged_in? && current_user == @user %>
  <div class="text-center mt-4"><%= link_to "Edit your profile", edit_user_path(@user) %></div>
<% end %> 
<h3 class="text-center">Articles</h3>

<%= render @articles %>
